"""Contexto de domínio `governanca` — políticas de plataforma (SDD §2.1 Domain, §4.1).

No M4 só a política publicada v1 (seed §11.4) é necessária, para o congelamento no GO
(`os.frozen.policy_version`); CRUD/draft→publicada chega com o M12 (§8-M12).
"""
