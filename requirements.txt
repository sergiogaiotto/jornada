# SDD §3.2 — requirements.txt inicial (backend); uma dependência por linha
fastapi~=0.115
uvicorn[standard]~=0.30
pydantic~=2.9
pydantic-settings~=2.5
sqlalchemy[asyncio]~=2.0
alembic~=1.13
asyncpg~=0.29
psycopg2-binary~=2.9
pgvector~=0.3
langgraph~=0.2
langgraph-checkpoint-postgres~=2.0
deepagents
openai~=1.50
httpx[http2]~=0.27
tenacity~=9.0
zeep~=4.2
# numpy NÃO é opcional (emenda F01 — UAT #5 onda 1). O §6/M8-A1 promete que a mesma
# seed reproduz os mesmos P10/P50/P90, mas `adapters/aleatorio.rng_disponivel()` troca
# de ALGORITMO conforme o numpy esteja instalado ou não — e ele não constava daqui. Na
# prática o mesmo grafo com a mesma seed dava conversões P50 1625,0 numa máquina com
# numpy e 1736,5 no CI/VPS sem ele: o "Previsto congelado" que serve de régua pós-
# lançamento (§1.1.2) dependia de uma dependência acidental. Fixado no caminho
# recomendado do §6 (binomial exata vetorizada), igual em dev, CI e produção.
numpy~=1.26
# Validação REAL do jgc.schema.json (onda 5 — I01, o P2 do UAT #5). O schema era a
# "fonte da verdade" que ninguém executava: toda restrição de valor (faixas, enums,
# tipos, additionalProperties) era decorativa, e a VPS aceitou STO de -72h, metrica
# "telepatia" e throttle "mil" com 201. Draft 2020-12, o mesmo declarado no schema.
jsonschema~=4.23
langfuse~=2.53
python-jose[cryptography]~=3.3
# Hash de senha argon2id (emenda G01 — identidade real por trás do RBAC §8-M0). Vencedor
# da Password Hashing Competition e recomendação corrente da OWASP: memory-hard, custo
# parametrizável e `check_needs_rehash` embutido (reparametrizar não invalida os hashes
# antigos — eles migram no próximo login bem-sucedido).
argon2-cffi~=23.1
structlog~=24.4
python-docx~=1.1
orjson~=3.10
pytest~=8.3
pytest-asyncio~=0.24
respx~=0.21
ruff~=0.6
mypy~=1.11
